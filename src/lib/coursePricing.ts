/**
 * Kept for existing imports. The catalogue in `@/lib/catalogue` is the single
 * source of truth for prices and price IDs.
 */
export {
  COURSE_PRICE_IDS,
  getCoursePriceId,
  getLevelBundlePriceId as getBundlePriceId,
} from "@/lib/catalogue";
